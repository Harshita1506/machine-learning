#when we open and clsoe files explicitly we use with keyword
with open("PrimeClasses\\fundamentals_5\\sample.txt","r") as f :
    print(f.read())
    #print((len(data)))
    # we dont need to explicitly close the file with will handle it 





